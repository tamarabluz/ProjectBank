spring:
  application:
    name: mscclients

server:
  port: 0

eureka:
  client:
    server-url:
      defaultZone: hhtp://localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
